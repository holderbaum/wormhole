= Wormhole

== Testing

  bundle install
  rake spec

== TODO

* Seed Wormhole::Config with (nested) values:
    Wormhole::Config.new(:key => { :subkey => :value })
* Add Wormhole::Config#to_hash
+ Add Wormhole::Config#each (include Enumerable)
