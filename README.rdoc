= Wormhole

== Testing

  gem install bundler
  bundle install
  rake

== TODO

* Seed Wormhole::Config with (nested) values:
    Wormhole::Config.new(:key => { :subkey => :value })

* Define a sensible return value for +Wormhole.create+ 

